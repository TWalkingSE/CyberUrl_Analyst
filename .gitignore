# ── Ambiente e segredos ──
.env
*.env.local

# ── Python bytecode ──
__pycache__/
*.py[cod]
*.pyo
*.egg-info/

# ── Ambientes virtuais ──
.venv/
venv/
env/

# ── Build / empacotamento ──
dist/
build/
*.spec

# ── Logs ──
*.log

# ── Datasets (manter apenas amostras) ──
datasets/downloads/
!datasets/*_sample.csv

# ── Modelo ML treinado (gerado localmente) ──
models/trained_model.joblib

# ── Dados persistentes locais ──
data/analysis_history.json
data/session_stats.json
data/quiz_leaderboard.json
data/user_feedback.json
# Manter data/scenarios.json (dados da aplicação)

# ── IDEs ──
.idea/
.vscode/

# ── Cache e temporários ──
.pytest_cache/
*.bak
*.tmp
*.swp

